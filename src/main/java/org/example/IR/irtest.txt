процедура строка проверка(целое x, целое y):
    если x > y тогда:
        вернуть "x больше y";
    иначе_если x < y тогда:
        вернуть "x меньше y";
    иначе:
        если x == 0 тогда:
            вернуть "x равно 0";
        иначе_если y == 0 тогда:
            вернуть "y равно 0";
        иначе:
            вернуть "x равно y";
конец_процедуры
процедура целое пример6(целое а):
    целое x
    вернуть x
конец_процедуры
процедура целое пример5(строка а):
    а = а + "123"
    целое x
    вернуть x
конец_процедуры

целое о = пример6(пример5("123"));

логическая х = правда || ((2+3)*5 < 4 && (11*5) <= 12)

целое я = ничего

цикл (целое i=0; i<6; i=i+1):
    я = я + "2"