процедура целое сложноеВычисление (целое а, целое б, целое в, целое г, целое д):
    целое результат = 0
    если (б != 0) тогда:
        результат = (а + б) * (в - г) / д
        вернуть результат;
    иначе:
        результат = -1
        вернуть результат;
    вернуть результат;
конец_процедуры
процедура целое цикл_сумма():
    целое сумма = 0
    цикл (целое y = 1; y <= 10; y = y + 1):
        цикл (целое i = 1; i <= 10; i = i + 1):
            сумма = сумма + i
    вернуть сумма;
конец_процедуры
процедура целое арифметикаИБулевы(целое x, целое y):
    целое z = 0
    если (x > y && !(x == y)) тогда:
        z = ((x + y) * (x - y)) / y % x
    иначе:
        z = -1
    вернуть z
конец_процедуры
процедура целое логика():
    целое a = 7
    целое b = 6
    целое результат = 0

    если a > b тогда:
        результат = 1
        вернуть результат;
    иначе_если a == b тогда:
        результат = 2
        вернуть результат;
    иначе:
        результат = 3
        вернуть результат;
    вернуть результат;
конец_процедуры
процедура целое факториал(целое число):
    если число == 1 тогда:
        вернуть 1
    вернуть факториал(число-1) * число
конец_процедуры
процедура целое сумма (целое а, целое б):
    целое в = а + б
    вернуть в;
конец_процедуры
процедура целое разность (целое а, целое б):
    целое в = а - б
    вернуть в;
конец_процедуры
процедура целое умножение (целое а, целое б):
    целое в = а * б
    вернуть в;
конец_процедуры
процедура целое деление (целое а, целое б):
    целое в = а / б
    вернуть в;
конец_процедуры

целое а = 200
целое б = 400
целое в = 150
целое г = 200
целое д = 20
целое результат = 0
строка гол = "qwety"

результат = сумма(а,б)
вывод(результат)

результат = разность(а,б)
вывод(результат)
результат = умножение(а,б)
вывод(результат)
результат = деление(а,б)
вывод(результат)
результат = факториал(10)
вывод(результат)
результат = сложноеВычисление(а,б,в,г,д)
вывод(результат)
результат = цикл_сумма()
вывод(результат)
результат = арифметикаИБулевы(а, б)
вывод(результат)
результат = арифметикаИБулевы(25, 12)
вывод(результат)
результат = логика()
вывод(результат)
